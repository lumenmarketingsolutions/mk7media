"""WhatsApp Observer: the no-reply attribution product.

Watches a client's WhatsApp conversations (Meta coexistence), decides when a lead
qualifies and when a sale happens, and sends those as Conversions API events so the
client's ads optimise toward buyers. It never sends a message to a customer.

Nothing in this package is specific to any business. Everything a business needs is a
JSON file in clients/, produced from the intake questions in INTAKE.md.
"""
